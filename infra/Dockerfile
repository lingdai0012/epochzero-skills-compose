# Application container
#
# Build stages:
#   1. Base: Python 3.11 slim
#   2. Dependencies: Install from pyproject.toml
#   3. Application: Copy source code
#
# Entrypoint: auto-skill-gen CLI or FastAPI server
#
# Usage:
#   docker build -t auto-skill-generator .
#   docker run auto-skill-generator run --help
