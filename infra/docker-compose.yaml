# Docker Compose for local development
#
# Services:
#   qdrant:
#     - Vector database for chunk embeddings
#     - Port: 6333 (REST API), 6334 (gRPC)
#
#   redis:
#     - Task queue backend for async processing
#     - Port: 6379
#
# Usage:
#   docker compose up -d
#   docker compose down
