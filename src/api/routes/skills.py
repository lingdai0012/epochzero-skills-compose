"""Skill CRUD and browsing endpoints.

Endpoints to implement:
    - GET  /api/skills                         — List all skills with status and filters
    - GET  /api/skills/{name}                  — Get skill detail + version history
    - GET  /api/skills/{name}/diff/{v1}/{v2}   — Diff between two versions
"""
