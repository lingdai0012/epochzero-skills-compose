"""Review queue and approval endpoints.

Endpoints to implement:
    - GET  /api/review/queue              — Skills pending review, sorted by priority
    - POST /api/review/{name}/approve     — Approve a skill version for publishing
    - POST /api/review/{name}/reject      — Reject with comments
    - POST /api/review/{name}/edit        — Submit manual edits to a skill
"""
