"""System health and skill performance metrics endpoints.

Endpoints to implement:
    - GET /api/metrics — System health metrics:
        total skills, pending reviews, recent changes,
        trigger counts, quality scores, freshness stats
"""
